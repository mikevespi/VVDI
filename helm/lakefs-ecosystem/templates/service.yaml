apiVersion: v1
kind: Service
metadata:
  name: {{ include "lakefs-ecosystem.fullname" . }}
  labels:
    {{- include "lakefs-ecosystem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lakefs-ecosystem.selectorLabels" . | nindent 4 }}
